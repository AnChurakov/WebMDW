@using Microsoft.AspNet.Identity.EntityFramework;
@model IEnumerable<IdentityRole>

<h2>Роли</h2>

<div class="row">
    <div class="col-md-10">
        @foreach (var role in Model)
         {
            <div class="d-inline p-2 bg-dark text-white">@role.Name</div>
        }
    </div>
</div>